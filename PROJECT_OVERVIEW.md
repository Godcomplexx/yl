# Технический Обзор Проекта: Cinematic Dataset Collector

## Архитектура и Структура Проекта

### Общая Архитектура
Проект построен по модульному принципу с четким разделением ответственности:
- **Конфигурация**: Централизованная настройка через `config.yaml`
- **Сбор данных**: Паттерн "Стратегия" для различных источников видео
- **Обработка**: Модульная система обработки видео
- **Логирование**: Единая система логирования
- **Основной пайплайн**: Координация всех компонентов

---

## 1. Конфигурация (`config.yaml`)

### Ключевые параметры:
- **`target_clip_count: 1000`** - Целевое количество клипов в датасете
- **`video_margin_factor: 1.5`** - Коэффициент запаса для скачивания
- **`keywords`** - 10 ключевых слов для поиска кинематографичных видео
- **`active_scrapers`** - Активные стратегии сбора (YouTube, TikTok)
- **`scraper.max_video_duration: 60`** - Максимальная длительность исходного видео
- **`processing.clip_duration: 5`** - Длительность финальных клипов

### Структура путей:
```yaml
paths:
  dataset_dir: 'dataset'
  raw_videos_dir: 'temp/raw_videos'
  logs_dir: 'logs'
  hashes_file: 'temp/processed_hashes.txt'
  index_file: 'dataset/index.csv'
```

---

## 2. Система Логирования (`src/utils/logger.py`)

### Функциональность:
- **Двойной вывод**: Консоль + ротируемый файл
- **Ротация логов**: 10MB файлы, 5 резервных копий
- **Единый логгер**: `'CinematicDatasetCollector'` для всего проекта
- **Предотвращение дублирования**: Очистка обработчиков при повторной инициализации

### Код:
```python
def setup_logger(logs_dir=Path('logs'), log_file='run.log'):
    logger = logging.getLogger('CinematicDatasetCollector')
    # Настройка консольного и файлового обработчиков
```

---

## 3. Модуль Сбора Данных (`src/scrapers/`)

### Паттерн "Стратегия":
- **`base_scraper.py`**: Абстрактный базовый класс `ScraperStrategy`
- **`youtube_scraper.py`**: Реализация для YouTube через `yt-dlp`
- **`tiktok_scraper.py`**: Реализация для TikTok через RapidAPI
- **`__init__.py`**: Экспорт классов стратегий

### YouTube Scraper:
```python
class YouTubeScraper(ScraperStrategy):
    def search_and_download(self, keyword, config, download_dir):
        # 1. Поиск метаданных без скачивания
        # 2. Фильтрация по длительности
        # 3. Скачивание отфильтрованных видео
```

### TikTok Scraper:
```python
class TikTokScraper(ScraperStrategy):
    def search_and_download(self, keyword, config, download_dir):
        # 1. Запрос к RapidAPI
        # 2. Парсинг JSON ответа
        # 3. Скачивание видео по прямым ссылкам
```

---

## 4. Модуль Обработки Видео (`src/processing.py`)

### Основные функции:

#### Управление хешами:
- **`load_hashes()`** - Загрузка существующих хешей из файла
- **`save_hash()`** - Сохранение нового хеша
- **`is_duplicate()`** - Проверка на дубликаты
- **`calculate_video_hash()`** - Вычисление MD5 хеша файла

#### Обработка видео:
- **`get_video_duration()`** - Получение длительности через ffmpeg
- **`trim_video()`** - Нарезка случайного фрагмента заданной длительности
- **`detect_watermark()`** - Детекция водяных знаков через OpenCV

### Алгоритм детекции водяных знаков:
1. Извлечение двух кадров из разных временных точек
2. Поиск ключевых точек алгоритмом ORB
3. Сопоставление точек и вычисление гомографии
4. Выравнивание кадров и вычисление разности
5. Анализ статичных областей (потенциальные водяные знаки)

---

## 5. Основной Пайплайн (`src/main.py`)

### Архитектура main.py:
- **27,273 байта** - самый большой файл проекта
- **Функция `main()`** - основная логика пайплайна
- **Функция `process_video()`** - обработка отдельного видео
- **Функция `generate_report()`** - создание отчета

### Логика работы пайплайна:
1. **Загрузка конфигурации** из `config.yaml`
2. **Инициализация логгера** и создание директорий
3. **Загрузка существующих хешей** для дедупликации
4. **Подсчет существующих клипов** в датасете
5. **Обработка существующих видео** (если есть)
6. **Цикл скачивания новых видео** до достижения целевого количества
7. **Создание индекса и отчета**
8. **Архивация датасета**

### Система прогресса:
- **`scraping_progress.txt`** - прогресс скачивания
- **`processing_progress.txt`** - прогресс обработки
- **Резервное копирование** прогресса при сбросе
- **Возобновление работы** после прерывания

---

## 6. Анализ Кода и Выявленные Проблемы

### ✅ Хорошие Практики:
1. **Модульная архитектура** - четкое разделение ответственности
2. **Паттерн "Стратегия"** - легкое добавление новых источников
3. **Единая система логирования** - централизованное логирование
4. **Обработка ошибок** - try/catch блоки во всех критических местах
5. **Конфигурируемость** - все параметры в config.yaml
6. **Прогресс-трекинг** - возможность возобновления работы

### ⚠️ Выявленные Проблемы:

#### 1. Дублирование Логгера:
```python
# В каждом файле повторяется:
log = logging.getLogger('CinematicDatasetCollector')
```
**Найдено в**: `youtube_scraper.py`, `tiktok_scraper.py`, `processing.py`

#### 2. Повторяющиеся Импорты:
```python
# Стандартные импорты повторяются:
import logging
from pathlib import Path
from typing import List, Dict
```

#### 3. Дублирование Обработки Path:
```python
# В processing.py повторяется преобразование Path в строку:
filepath_str = str(filepath)
input_path_str = str(input_path)
output_path_str = str(output_path)
```

#### 4. Устаревший Файл:
- **`src/scraper.py`** - помечен как DEPRECATED, но не удален

#### 5. Жестко Закодированные Значения:
- API ключ RapidAPI в конфиге (должен быть в переменных окружения)
- Магические числа в алгоритмах (пороги, таймауты)

### 🔧 Рекомендации по Улучшению:

#### 1. Создать Общий Модуль Утилит:
```python
# src/utils/common.py
def get_logger(name='CinematicDatasetCollector'):
    return logging.getLogger(name)

def path_to_str(path):
    return str(path) if isinstance(path, Path) else path
```

#### 2. Вынести Константы:
```python
# src/constants.py
LOGGER_NAME = 'CinematicDatasetCollector'
DEFAULT_CLIP_DURATION = 5
WATERMARK_THRESHOLD = 0.05
```

#### 3. Улучшить Управление Конфигурацией:
```python
# Использовать переменные окружения для секретов
rapidapi_key = os.getenv('RAPIDAPI_KEY', config.get('rapidapi', {}).get('key'))
```

#### 4. Добавить Валидацию Конфигурации:
```python
def validate_config(config):
    required_keys = ['target_clip_count', 'keywords', 'active_scrapers']
    for key in required_keys:
        if key not in config:
            raise ValueError(f"Missing required config key: {key}")
```

---

## 7. Метрики Проекта

### Размеры Файлов:
- **`main.py`**: 27,273 байта (основная логика)
- **`processing.py`**: 7,962 байта (обработка видео)
- **`youtube_scraper.py`**: 4,691 байта
- **`tiktok_scraper.py`**: 5,825 байта
- **`logger.py`**: 1,012 байта
- **`base_scraper.py`**: 718 байта

### Общая Статистика:
- **Общий размер кода**: ~47KB
- **Количество модулей**: 6 основных файлов
- **Количество функций**: ~15 основных функций
- **Паттерны проектирования**: Стратегия, Фабрика
- **Внешние зависимости**: 8 основных библиотек

---

## 8. Заключение

Проект демонстрирует хорошую архитектуру с четким разделением ответственности. Основные сильные стороны:
- Модульность и расширяемость
- Надежная обработка ошибок
- Система прогресс-трекинга
- Гибкая конфигурация

Основные области для улучшения:
- Устранение дублирования кода
- Улучшение управления секретами
- Добавление валидации конфигурации
- Очистка устаревшего кода

В целом, проект готов к продуктивному использованию и может быть легко расширен для поддержки новых источников видео.ылок без водяных знаков.

- **`__init__.py`**: Делает эту папку Python-пакетом и экспортирует классы скраперов.

**Как это работает:**

1. В `config.yaml` появился новый список `active_scrapers`.
2. `main.py` читает этот список и для каждого элемента (например, `'youtube'`) находит соответствующий класс (например, `YouTubeScraper`).
3. Создается экземпляр этого класса, и вызывается его метод `search_and_download`.

**Преимущество:** Чтобы добавить поддержку новой платформы, достаточно создать новый файл (например, `tiktok_scraper.py`), реализовать в нем свою логику и просто добавить `'tiktok'` в список `active_scrapers` в конфиге. Остальной код менять не придется.

---

## 4. `src/processing.py` - Модуль Обработки Видео

**Задача:** Превратить сырые скачанные видео в качественные, уникальные клипы.

**Ключевые функции:**

- **`trim_video`**: Из каждого длинного видео вырезается случайный фрагмент заданной длительности (`clip_duration`).
- **`calculate_video_hash`**: Для каждого клипа вычисляется перцептивный хеш с помощью библиотеки `videohash`. Этот хеш представляет собой "цифровой отпечаток" видео.
- **`is_duplicate`**: Сравнивает хеш нового клипа с хешами уже добавленных в датасет. Если находится слишком похожий, клип помечается как дубликат и отбрасывается.
- **`detect_watermark`**: Самая сложная функция в модуле. Она реализует "умную" детекцию водяных знаков с помощью `OpenCV`:

  1. Берет два кадра из разных частей клипа.
  2. Находит на них общие характерные точки (features) с помощью алгоритма ORB.
  3. Сопоставляет эти точки и вычисляет матрицу гомографии, чтобы "совместить" один кадр с другим, компенсируя движение камеры.
  4. Один кадр "накладывается" на другой с учетом этого движения (вычисляется гомография).
  5. Вычисляется разница между совмещенными кадрами. В результате движущиеся объекты сцены исчезают, а статичные элементы (водяные знаки) остаются видимыми.
  6. Если площадь таких статичных областей превышает заданный порог, видео помечается как содержащее водяной знак и отбраковывается.

---

## 5. `src/main.py` - Главный Оркестратор

**Задача:** Управлять всем процессом от начала до конца.

**Логика функции `main`:**

1. **Инициализация:** Загружает `config.yaml`, настраивает логгер с помощью `setup_logger`.
2. **Скачивание:** Динамически запускает скраперы, указанные в `active_scrapers` из конфига, для сбора сырых видео.
3. **Основной цикл обработки:**
    - Итерируется по каждому скачанному видео.
    - Вызывает `trim_video` для нарезки клипа.
    - Вызывает `detect_watermark` для проверки на наличие водяных знаков. Если найден — переход к следующему видео.
    - Вызывает `calculate_video_hash` и `is_duplicate` для проверки на уникальность. Если дубликат — переход к следующему видео.
    - Если все проверки пройдены, клип перемещается в финальную папку (`dataset/clips/<keyword>`), а его метаданные добавляются в список для будущего `index.csv`.
4. **Финализация:**
    - Создает `index.csv` из собранных метаданных.
    - Генерирует `report.md` со статистикой (сколько клипов собрано по каждому тегу).
    - Упаковывает всю папку `dataset` в `dataset.zip`.
    - Удаляет временные папки.

---

## 6. Интеграция с RapidAPI

Проект теперь включает интеграцию с RapidAPI для доступа к платформе TikTok:

- **Конфигурация:** В `config.yaml` добавлена секция `rapidapi` для хранения API-ключа.
- **Использование в скраперах:** Скрапер TikTok использует этот ключ для доступа к API TikTok через RapidAPI.
- **Преимущества:** Позволяет получать видео без водяных знаков и с более точными метаданными.
- **Расширяемость:** Та же интеграция может быть использована для добавления других платформ (Instagram Reels, Vimeo и т.д.).

---

## 7. Категоризация и Метаданные

Проект обеспечивает тщательную категоризацию и управление метаданными:

- **Структура папок:** Клипы организованы в папки по ключевым словам/тегам (`dataset/neon_city/`, `dataset/action_scene/` и т.д.).
- **Индексный файл:** Создается `index.csv`, содержащий все метаданные для каждого клипа:
  - ID клипа
  - Тег/категория
  - Длительность
  - Исходный URL
  - Перцептивный хеш для идентификации
- **Отчет:** Генерируется `report.md` с общей статистикой по собранному датасету.

---

## 8. Расширение Проекта

Проект спроектирован с учетом масштабируемости. Для добавления новых источников видео:

1. Создайте новый класс скрапера, наследующийся от `ScraperStrategy`.
2. Реализуйте метод `search_and_download`.
3. Добавьте новый класс в словарь `AVAILABLE_SCRAPERS` в `main.py`.
4. Добавьте новый источник в список `active_scrapers` в `config.yaml`.

Аналогично можно расширять функциональность обработки, добавляя новые методы фильтрации и улучшения в `processing.py`.
